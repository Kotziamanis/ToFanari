Synch-tool
==========

This is a separate application from:

  • PDF Marker (main.py)
  • Tofanari Main Tool (app.py)

The Synch-tool codebase is not kept in this folder. This directory only reserves the name
so we do not mix it with the other two apps.

When the Synch-tool project exists, keep it in its own repository or folder.
