# output_ready (generated)

This folder is produced by:

```bash
python app.py prepare_bunny
```

Upload the contents to Bunny Storage under the same path layout as your CDN Pull Zone (e.g. `books/<book_slug>/<chapter_slug>/`).

You can delete and regenerate this folder anytime; it is safe to re-run the prepare step.
